# Language interpreters are defined with the following properties:
# * language: the name of the language
# * interpreter: the name of the Java bean

plugin.language.runtime[0].language=python
plugin.language.runtime[0].interpreter=pythonPluginInterpreter
