import { LibraryFilterPipe } from './library-filter.pipe';

describe('LibraryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibraryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
